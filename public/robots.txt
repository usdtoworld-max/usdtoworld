User-agent: *
Allow: /

# Additional AI-crawler blocks not already covered by Cloudflare's Managed
# robots.txt / AI Crawl Control feature (Security > AI Crawl Control in the
# Cloudflare dashboard). If that feature is enabled for this zone, Cloudflare
# prepends its own block ABOVE this file's content at the edge — it already
# includes User-agent: * / Content-Signal, plus Disallow rules for GPTBot,
# Google-Extended, CCBot, ClaudeBot, Bytespider, Amazonbot,
# Applebot-Extended, and meta-externalagent. This file intentionally does
# NOT repeat those, since restating them just produces duplicate
# User-agent/Disallow groups in the final served robots.txt (which is what
# was getting flagged) without changing the actual policy — Cloudflare's
# injected rules apply either way. Only bots Cloudflare's managed block
# doesn't cover are listed here.
User-agent: ChatGPT-User
Disallow: /

User-agent: Claude-Web
Disallow: /

User-agent: PerplexityBot
Disallow: /

Sitemap: https://usdtoworld.com/sitemap-index.xml
